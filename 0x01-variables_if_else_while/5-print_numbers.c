#include <stdio.h>

/**
 * main - Prints single digit nos of base 10
 *
 * Return: 0 (Success)
 *
 */
int main(void)
{
	int f;

	for (f = 0; f < 10; f++)
		printf("%d", f);
	putchar('\n');
	return (0);
}
